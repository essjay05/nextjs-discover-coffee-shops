import utilStyles from "../../styles/utils.module.css"
import styles from "./Banner.module.css"

 const Banner = () => {
  return (
    <section className={styles.container}>
      <span className={utilStyles.heading2Xl}>Banner</span>
    </section>
  )
}

export default Banner